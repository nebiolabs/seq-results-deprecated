<ol class="breadcrumb">
  <li>{{#link-to 'runs'}}Runs{{/link-to}}</li>
</ol>

<div class="container-fluid">
  <div class="row-fluid">
    <div class="col-md-6">
      <h3>
        <span class="label label-default">Runs</span>
      </h3>

      <table class='table table-striped'>
        <thead>
          <tr>
            <th>Name</th>
            <th>Instrument</th>
            <th>Instrument Type</th>
          </tr>
        </thead>

        <tbody>
          {{#each}}
            {{#link-to 'run' this tagName="tr"}}
              <td>{{name}}</td>
              <td>{{instrument}}</td>
              <td>{{instrument_type}}</td>
            {{/link-to}}
          {{/each}}
        </tbody>
      </table>
    </div>

    <div class="col-md-6">
      {{outlet}}
    </div>
  </div>
</div>